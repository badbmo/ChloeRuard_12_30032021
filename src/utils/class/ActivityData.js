export default class ActivityData {
	/**
	 * User Activitiy Data
	 * @param { number } id
	 * @param { array } sessions
	 */
	constructor(id, sessions) {
			this.id = id;
			this.sessions = sessions;
	}
}