export default class SessionsData {
	/**
	 * User Sessions Data
	 * @param { number } id
	 * @param { array } sessions
	 */
	constructor(id, sessions) {
			this.id = id;
			this.sessions = sessions;
	}
}